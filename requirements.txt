# Core Django & Wagtail
Django>=5.0,<5.2  # Django 5.0+ required for Python 3.14 compatibility
wagtail>=6.0,<7.0  # Upgraded for Python 3.14 compatibility

# Database drivers
oracledb  # Oracle database
psycopg2-binary>=2.9.9  # PostgreSQL (optional for dev)

# Production server
gunicorn>=21.2.0
whitenoise>=6.6.0  # Static file serving

# Payment gateway
razorpay>=1.4.1

# PDF Generation
reportlab>=4.0.0
weasyprint>=60.0  # Alternative PDF generation

# Image handling
Pillow>=10.0.0,<12.0.0  # Compatible with Wagtail 6.x and Python 3.14

# Environment management
python-decouple>=3.8

# Monitoring & Error tracking (optional)
sentry-sdk>=1.39.1

# Storage (for production media files)
django-storages[boto3]>=1.14.2

# Utilities
django-extensions>=3.2.3
django-cors-headers>=4.3.1

# Development
django-debug-toolbar>=4.2.0
